export enum E_APP {
  NAME = "Khattabat",
  DEFAULT_USER_NAME = "User",
  CHAT_AVATAR_INITIAL = "C",
}

export enum E_ICON {
  CART = "🛒",
  CHAT = "💬",
}

export enum E_PLACEHOLDER {
  NAME = "John Doe",
  EMAIL = "email@example.com",
  PASSWORD = "••••••••",
}

export enum E_LOG_ERROR {
  STORAGE_SET = "couldnt set in local storage",
  FETCH_ITEMS = "Failed to fetch items",
}
